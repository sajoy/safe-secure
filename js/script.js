console.log('hey')

/* 

- autopopulate config JSON with default config (save to localstorage)

- prevent submit
- get config JSON
- save config JSON to localstorage

- pick initial number of shapes (1-7)
- for each shape pick
    - color
    - size
    - shape
    - position

- return array of info
- draw
    - how to draw shapes? canvas v divs
        - are divs more accessible? (describe individual pieces vs/+ whole?)
        - use images? svgs (so can scale nicely)?
    - simplify to very simple shapes?
    - add info to JSON of link to image?


*/